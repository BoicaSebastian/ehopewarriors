import styled, { themeGet, device } from "@theme/utils";

export const ServiceAare = styled.section`
    padding: 65px 0 0;
    position: relative;
    z-index: 1;
    margin-bottom: 50px;
    ${device.small} {
        padding: 135px 0 0;
    }
`;
